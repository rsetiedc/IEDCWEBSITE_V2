name: Deploy Vite to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    environment: github-pages
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Check Baserow token configured
        env:
          VITE_BASEROW_TOKEN: ${{ secrets.VITE_BASEROW_TOKEN }}
        run: |
          if [ -z "$VITE_BASEROW_TOKEN" ]; then
            echo "::error::The VITE_BASEROW_TOKEN secret is not set, so the deployed Events page cannot live-fetch from Baserow. Add a read-only token under Settings -> Secrets and variables -> Actions, then re-run this workflow."
            exit 1
          fi

      - name: Generate events snapshot
        run: node scripts/generateEventsJson.mjs
        env:
          # Optional — the script falls back to the default Baserow database
          # and table IDs baked into src/services/baserow.js if these are unset
          # (set the token under Settings → Secrets, the IDs under Variables).
          VITE_BASEROW_TOKEN: ${{ secrets.VITE_BASEROW_TOKEN }}
          VITE_BASEROW_EVENTS_TABLE_ID: ${{ vars.VITE_BASEROW_EVENTS_TABLE_ID }}
          VITE_BASEROW_CONTACTS_TABLE_ID: ${{ vars.VITE_BASEROW_CONTACTS_TABLE_ID }}

      - name: Verify events snapshot
        run: |
          COUNT=$(node --input-type=module -e "import { readFileSync } from 'node:fs'; const e = JSON.parse(readFileSync('./public/events.json', 'utf8')); console.log(e.length)")
          echo "events.json contains $COUNT events"
          if [ "$COUNT" -eq 0 ]; then
            echo "::error::events.json is EMPTY. Check that the Baserow database token is valid (or commit a populated public/events.json)."
            exit 1
          fi

      - name: Build project
        run: npm run build
        env:
          # Pass the Baserow credentials into the client bundle so the deployed
          # site reads events LIVE from Baserow (the Events page auto-refreshes
          # every 15s) instead of only using the deploy-time snapshot. The token
          # is embedded in the public JS bundle, so keep it scoped to read-only.
          VITE_BASEROW_TOKEN: ${{ secrets.VITE_BASEROW_TOKEN }}
          VITE_BASEROW_EVENTS_TABLE_ID: ${{ vars.VITE_BASEROW_EVENTS_TABLE_ID }}
          VITE_BASEROW_CONTACTS_TABLE_ID: ${{ vars.VITE_BASEROW_CONTACTS_TABLE_ID }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
