# Events are read from this publicly shared Google Sheet (CSV export, no API key needed).
VITE_EVENTS_SHEET_ID=1rZUeS3HQUzrKHRNR1nplt5cdkHWK0h-wDOsjmGSSHp4
VITE_EVENTS_SHEET_GID=311663456

# Optional: how often (in ms) the Events page re-checks the sheet for changes.
# Defaults to 60000 (every minute). Set to 0 to disable auto-refresh.
VITE_EVENTS_REFRESH_MS=60000
